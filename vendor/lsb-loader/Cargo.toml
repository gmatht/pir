[package]
name = "lsb-loader"
version = "0.1.0"
edition = "2021"
description = "Runtime dynamic loader for LSB system libraries with trust checks"
license = "MIT"

[dependencies]
libloading = "0.8"
thiserror = "1.0"
libc = "0.2"

[lib]
name = "lsb_loader"
path = "src/lib.rs"
