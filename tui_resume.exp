#!/usr/bin/expect -f
set timeout 60
spawn /root/src/pir/target/debug/pir --tui -r 20260828-123600
# wait for TUI to render
sleep 2
# press Up arrow to recall last prompt; then a marker to reveal it
send "\033\[A"
sleep 1
send "ZZ"
sleep 1
# quit
send "\x04"
sleep 1
expect eof
