# Rust build output
/target

# Local project metadata / session logs written next to the cwd
/.pir

# Editor / OS noise
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
*.iml

# Local env overrides
.env
.env.local
fuzz/fuzz_parser
fuzz/corpus/
/.rustxWidgets-upstream/

# Optional GUI dependency (rustxwidgets) — NOT vendored in this repo.
# Check it out as a sibling (/home/ai_pir/rustxWidgets-canonical or /root/src/rustxWidgets)
# and add a [patch] path entry to build --features gui/pancurses.
rustxWidgets
rustxWidgets/

# Local scratch artifacts to sibling worktrees, backups, archives)
rustxWidgets.bak
pir_.gz
build-gui.sh
