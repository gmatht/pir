* Audit codebase, replace polling and esp. Busy waits with smol
   - Document any remaining polling and busywaits and why they are hard to remove.

- Continue developing GUI.

/thinking

Debug why /model op completes to something random...

opencode extension?.

Worktree pruning.


Automatically detect SSH and github prompts?
.ssh and github tokens as FIFO, wait for user approval?
- REPL-first startup: show the idle prompt BEFORE worktree/quarantine setup. Create
  the worktree + quarantine in a background task and swap cwd in when ready
  (block any tool call until ready). Currently setup is synchronous but fast
  (~0.1s) since the startup network fetch was removed.
- Container read-isolation: hide ~/.ssh, gh auth tokens (~/.config/gh), and other
  credential stores from the agent's view (bind empty dirs over them inside the
  container/overlay). LATER.
- rustxWidgets: make it a git SUBTREE shared with ~/src/main_loop/corro (currently an
  untracked vendored dir, gitignored; /mnt/c/temp/pir carries a NEWER uncommitted
  rustxwidgets — subtree it from a canonical rustxWidgets repo so both projects and
  worktrees get one version). Worktrees already symlink it (link_untracked_build_deps).
